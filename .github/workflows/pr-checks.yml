name: pr-checks

on:
  pull_request:
    types: [opened, edited, synchronize, reopened]

permissions:
  pull-requests: read
  contents: read

# Dependabot PRs skip the human-targeted gates (handoff, conv-title strict).
# CI still runs (real signal), and dependabot's own commit titles are CC-compliant
# but use scopes outside our list (`deps-dev`, raw `actions`).

jobs:
  conv-title:
    if: github.actor != 'dependabot[bot]'
    runs-on: [self-hosted, linux, ovh, starter]
    steps:
      - uses: amannn/action-semantic-pull-request@v5
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          types: |
            feat
            fix
            refactor
            perf
            chore
            docs
            test
            build
            ci
            style
            revert
          scopes: |
            auth
            billing
            db
            ui
            infra
            docs
            deps
            deps-dev
            ci
            tests
            gates
            agents
            epoch
            release
            actions
          requireScope: false
          subjectPattern: ^[a-z0-9].+$

  paired-files:
    runs-on: [self-hosted, linux, ovh, starter]
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: mikefarah/yq@v4
      - name: check-paired-files
        env:
          GITHUB_BASE_REF: ${{ github.base_ref }}
          GITHUB_HEAD_REF: ${{ github.head_ref }}
        run: bash .github/scripts/check-paired-files.sh

  handoff-gate:
    if: github.actor != 'dependabot[bot]'
    runs-on: [self-hosted, linux, ovh, starter]
    steps:
      - uses: actions/checkout@v4
      - name: verify-handoff
        env:
          BRANCH: ${{ github.head_ref }}
        run: |
          slug="${BRANCH//\//-}"
          file="docs/roadmap/handoffs/HANDOFF-$slug.md"
          if [ ! -f "$file" ]; then
            echo "::error::Missing handoff: $file"
            exit 1
          fi
          if grep -q "^name: <" "$file" 2>/dev/null; then
            echo "::error::Handoff still contains template placeholders: $file"
            exit 1
          fi

  size-cap:
    runs-on: [self-hosted, linux, ovh, starter]
    if: ${{ !contains(github.event.pull_request.labels.*.name, 'size-cap-override') }}
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: size-cap
        env:
          BASE: ${{ github.base_ref }}
        run: |
          # Counts insertions only (lines added). Deletions are cheap to review;
          # additions are the real review burden. Excludes generated/binary content
          # and auto-generated registry artifacts.
          loc=$(git diff --shortstat "origin/$BASE...HEAD" -- \
            ':!pnpm-lock.yaml' \
            ':!db/migrations/meta/**' \
            ':!**/*.svg' \
            ':!**/*.png' \
            ':!**/*.ico' \
            ':!src/components/__index__.tsx' \
            ':!src/components/**/examples/**' \
            ':!src/components/**/_fixtures/**' \
            ':!docs/showcase-export*' \
            ':!docs/components-manifest.json' \
            | awk '{print $4}')
          loc=${loc:-0}
          echo "loc added: $loc"
          if [ "$loc" -gt 2000 ]; then
            echo "::error::PR too large ($loc lines added). Split into smaller PRs, or apply 'size-cap-override' label if intentional."
            exit 1
          fi
          if [ "$loc" -gt 800 ]; then
            echo "::warning::PR is large ($loc lines added). Justify in description."
          fi
